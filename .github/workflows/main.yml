name: learn-github-actions
on: [push]
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: display
      - shell: bash
        run: |
          echo $Action $Object!
